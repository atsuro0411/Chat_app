<h1>ユーザー一覧</h1>

<% if @users.any? %>
  <ul>
    <% @users.each do |u| %>
      <li>
        <%= link_to u.name, user_path(u) %>
        <%= friendship_action_buttons(u) if defined?(friendship_action_buttons) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>ユーザーが見つかりませんでした。</p>
<% end %>